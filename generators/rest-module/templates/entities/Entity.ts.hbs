import {
  Entity,
  PrimaryGeneratedColumn,
  Column,
  CreateDateColumn,
  UpdateDateColumn,
} from 'typeorm';

import { {{Interface name}} } from '../interfaces/{{Interface name}}';
import { {{Interface name}}Extended } from '../interfaces/{{Interface name}}Extended';

@Entity('{{plural (snakeCase name)}}')
export class {{Entity name}} implements {{Interface name}}, {{Interface name}}Extended {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  name: string;

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;
}
